{
  "name": "zibe-firebase-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only database --project demo-zibe \"node --test tools/firebase-rules-tests/rules.test.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.1",
    "firebase-tools": "^13.12.0"
  }
}
