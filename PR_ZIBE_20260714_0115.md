# ✨ feat(ui): modernizar navegación, Descubrir y permisos

Base: `main` (`73bbfb36ddf5531603bd3be178d0d87ad7ecf298`)

Rama: `feature/ui-motion-discover-permissions`

## ✨ Qué se cambió

### Permisos y onboarding

- Mantiene el flujo secuencial de ubicación y notificaciones, con ubicación obligatoria y notificaciones opt-in.
- Conserva `NotificationPermissionState`, restauración de requests, Settings y prevención de prompts duplicados.
- No modifica onboarding, contratos de permisos ni SDK/toolchain en esta corrección visual.

### Navegación y Cuenta

- Mantiene el drawer eliminado y el orden `Descubrir`–`Chats`–`Salas`–`Favoritos`, con `Chats` como inicio.
- Muestra los cuatro destinos inferiores sólo como iconos, preservando títulos, content descriptions, selected state y badges para accesibilidad.
- Conserva el avatar de Cuenta, el bottom sheet y sus acciones; el borde visible ahora es blanco de `1.dp`, sin aro oscuro y sin reducir foto ni diámetro exterior.
- Mantiene badges, intents DM, Salas, Favoritos y el back stack raíz.

### Descubrir

- Conserva Compose dentro de `UsersFragment`; no restaura `RecyclerView`, `AdapterUsers` ni layouts XML activos.
- Recupera cards glass `#1AFFFFFF`, radio `20.dp`, avatar circular de `70.dp`, borde blanco, status dot online/offline y semántica de presencia.
- Restaura Poppins `18sp` para nombre/edad, `%d años`, descripción Poppins `13sp` de una línea y fallback `Perfil eliminado`.
- Extrae `UserStatusTag` y lo comparte con `ProfileCard` para distancia, favorito, bloqueos, silenciado y coincidencia de sala.
- Elimina la estrella y la edición de favoritos desde Descubrir; conserva el tag real de Favorito y la acción dentro del perfil.
- Mantiene el botón de chat de `48.dp`, loading, consulta de conversación y confirmación antes del primer DM.
- Mueve búsqueda a `action_search`/`SearchView` y filtros a la toolbar; el sheet concentra online y rango de edad.
- El filtro activo tiene una única fuente de verdad en `UsersUiState` y tiñe el icono con accent.
- Restaura `Volver arriba` con `animateScrollToItem(0)`, visibilidad por scroll y guard contra jobs simultáneos.
- Elimina completamente `Para vos` y su espacio; lista, loading, empty y error comienzan directamente debajo de la toolbar.
- Mantiene pull-to-refresh, retry y `Volver arriba`; no agrega refresh en toolbar.

### Filtros

- Mantiene online y rango de edad dentro del único sheet controlado por `UsersUiState`.
- Usa el token claro para el rango de edad y fija Cancelar/Aplicar fuera del contenido scrollable.
- El footer respeta navigation/IME insets y permanece accesible con altura compacta y font scale aumentado.

### Entrada a DM desde Descubrir y Perfil

- Centraliza la decisión en `ResolveDmEntryUseCase`: conversación existente abre directo; conversación nueva requiere confirmación; error no navega y muestra snackbar.
- Comparte un único `FirstContactSheet` entre Descubrir y Perfil, con el mismo copy y acciones.
- `ProfileViewModel` bloquea doble toque, conserva loading hasta estado terminal y emite navegación con UID explícito.
- El pager cancela consultas al perder la página activa, no muestra sheets offscreen y no reproduce navegación al reactivarse.
- Los accesos desde Chats/notificaciones continúan directos porque parten de conversaciones persistidas; `NODE_GROUP_DM` permanece intacto.

### ChatActivity

- Hace transparente `ChatTopBar` y la status bar para integrar el gradiente, tanto en modo normal como en selección.
- Retira `navigationBarsPadding()` de la barra superior y conserva back, perfil, overflow, selección y eliminación.

### Política de interacción

- Retira escala, hundimiento, bounce y háptica personalizada introducidos por el PR en cards, botones, filtros, Cuenta y bottom navigation.
- Usa ripple/pressed feedback y transiciones Material estándar, sin deformación geométrica.
- Elimina `ZibeMotion.kt` al quedar sin consumidores reales; no deja infraestructura muerta “para el futuro”.

## 🧭 Por qué

- La implementación inicial de Descubrir se alejaba del lenguaje visual de PERSONAS: cards navy altas, estado online como texto, buscador/filtros inline y favorito editable.
- La interacción personalizada sólo era visible en partes del producto y rompía consistencia.
- La toolbar necesitaba recuperar búsqueda y filtro sin sacrificar Cuenta ni el ancho disponible.

## 🔒 Contratos preservados

- Sin cambios en Functions, Rules, payloads FCM, paths RTDB ni backend.
- Se preservan `MSG_DELIVERED (1)`, `MSG_RECEIVED (2)`, `MSG_SEEN (3)`, monotonicidad, fan-out atómico, server timestamps y unread.
- `messaging.send()` no se interpreta como receipt; sólo Android receptor confirma `MSG_RECEIVED`.
- No cambia escrituras DM, primer mensaje, receipts, permisos, onboarding, Salas, Favoritos, dependencias, toolchain ni SDK levels.
- El ajuste de Perfil sólo unifica la decisión previa a abrir `ChatActivity`; no escribe mensajes ni crea conversaciones antes de confirmar.

## 📁 Archivos principales

- `app/src/main/java/com/zibete/proyecto1/ui/users/`: card Compose, estado del filter sheet, toolbar handlers y scroll-to-top.
- `app/src/main/java/com/zibete/proyecto1/ui/components/UserStatusTag.kt`: componente compartido nuevo para tags de usuario.
- `app/src/main/java/com/zibete/proyecto1/ui/components/ProfileCard.kt`: consumo del tag compartido.
- `app/src/main/java/com/zibete/proyecto1/ui/main/`: avatar de Cuenta, acciones de toolbar y tint de filtros.
- `domain/src/main/java/com/zibete/proyecto1/domain/chat/ResolveDmEntryUseCase.kt`: decisión tipada y compartida de entrada a DM.
- `app/src/main/java/com/zibete/proyecto1/ui/components/FirstContactSheet.kt`: aviso compartido de primer contacto.
- `app/src/main/java/com/zibete/proyecto1/ui/profile/`: gate de Perfil, UID explícito y lifecycle seguro del pager.
- `app/src/main/java/com/zibete/proyecto1/ui/chat/`: toolbar y status bar transparentes.
- `app/src/main/res/menu/menu_main.xml` y `app/src/main/res/drawable/ic_filter_list_24.xml`: acción e icono de filtro.
- `core/designsystem/src/main/res/values/dimens.xml`: tokens restaurados de `18sp`/`13sp`.
- `app/src/test` y `app/src/androidTest`: contratos visuales, toolbar, filtros, primer DM y scroll-to-top.
- `docs/ARCHITECTURE.md`: arquitectura real sin capa de motion personalizada.

### Código y recursos eliminados

- `core/designsystem/src/main/java/com/zibete/proyecto1/ui/motion/ZibeMotion.kt`.
- Callbacks, estado, strings y tests exclusivos de la estrella de favoritos en Descubrir.
- Consumidores de press-scale/háptica agregados por este PR.

## 🧪 Tests agregados o ampliados

- `DiscoverScreenAndroidTest`: glass/token, nombre, edad, descripción, avatar, presencia online/offline, semántica, tags, fallback, ausencia de favorito, chat, primer contacto y scroll-to-top.
- `MainNavigationAndroidTest`: búsqueda/filtro en toolbar, ausencia de refresh/hamburger, Cuenta, sheet, filtro activo y back de búsqueda.
- `DiscoverVisualContractTest`: tokens glass y tipografía legacy.
- `UsersViewModelTest`: estado del sheet, limpieza atómica de búsqueda/filtros y espera del estado terminal del aviso de primer contacto.
- `MainDestinationUiMapperTest`: `nav_users` habilita búsqueda y filtro sólo donde corresponde.
- `ResolveDmEntryUseCaseTest`: existente, nueva, failure, null y cancelación preservada.
- `ProfileViewModelTest`: loading, directo, sheet, confirmar, cancelar, error, doble toque y cancelación por cambio de página.
- El harness de `ProfileViewModelTest` espera la metadata completa con un valor centinela antes de terminar, evitando completions tardías de `Dispatchers.IO` después de `resetMain()` en CI.
- `ProfileScreenAndroidTest`: sheet compartido, conversación existente y página inactiva del pager.
- `ZibeBottomSheetAndroidTest`: contenido scrollable y footer fijo en altura compacta.
- `ChatTopBarAndroidTest` y `ChatChromeVisualContractTest`: transparencia, selección, status bar, overflow, back y perfil.

Resultado final: 115 tests JVM y 60 instrumentados, sin fallas ni skips.

## 🔎 Auditoría de consistencia

- Auditor inicial read-only: detectó bypass del aviso desde Perfil, riesgo de resultados cruzados entre páginas del pager, footer desplazable y contraste implícito del rango de edad.
- Correcciones aplicadas: gate tipado compartido, UID explícito, cancelación por página inactiva, sheet compartido, footer fijo y token claro explícito.
- Auditor final read-only: 0 bloqueantes y 0 hallazgos importantes; el único hallazgo importante inicial restante se cerró con `ProfileActivityContentAndroidTest` sobre el pager real.
- Hallazgos menores documentados: validación TalkBack física pendiente y warnings históricos fuera de alcance. No se detectaron bypasses nuevos hacia DM ni cambios en `NODE_GROUP_DM`.

## ✅ Validación local

```bash
python scripts/check_architecture.py
python -m py_compile functions/main.py
python -m unittest discover functions/tests
npm ci
npm run test:rules
gradlew.bat testDebugUnitTest --no-daemon
gradlew.bat :app:generateDebugAndroidTestLintModel :app:compileDebugAndroidTestKotlin --no-daemon
gradlew.bat lintDebug --rerun-tasks --no-daemon
gradlew.bat :app:assembleDebug --no-daemon
gradlew.bat :app:compileReleaseKotlin --no-daemon
gradlew.bat :app:connectedDebugAndroidTest --no-daemon
zipalign -c -P 16 -v 4 app/build/outputs/apk/debug/app-debug.apk
git diff --check
```

- Arquitectura: OK.
- Functions: compile OK; 6/6 tests OK.
- Rules RTDB: 49/49 tests OK.
- JVM: 115/115 tests OK, sin skips.
- Instrumentation: 60/60 en `Pixel_6a` API 37, sin skips.
- Lint: OK, 0 errores; 192 warnings históricos fuera del alcance y 0 warnings atribuibles en los archivos tocados.
- Debug APK, release Kotlin y modelo androidTest: OK.
- APK: alineación de páginas de 16 KB verificada.
- `npm ci`: OK; conserva warning de Node 24 fuera del engine de `superstatic` y avisos de paquetes deprecados existentes, sin cambios de dependencias.

## 📱 Validación visual y manual

- [x] Comparación contra las capturas PERSONAS anterior y DESCUBRIR previa a esta corrección.
- [x] Pixel_6a con fixture representativa: online, offline, favorito, bloqueado, descripción larga y perfil eliminado; silenciado cubierto por test de contrato visual.
- [x] Cards glass, densidad, Poppins 18sp/13sp, avatar, status dot, tags, edad, ellipsis y chat revisados en screenshot temporal.
- [x] Filter sheet revisado en screenshot real a 1080×2400: rango claro, contenido sin clipping y footer fijo por encima de navigation bar.
- [x] Aviso compartido de primer contacto y ChatTopBar transparente revisados en screenshots temporales del mismo AVD.
- [x] Toolbar real de `MainActivity`: título, Cuenta con borde blanco, búsqueda, filtro y overflow sin superposición.
- [x] `Volver arriba`, búsqueda/back, filter sheet, filtro activo, Cuenta y navegación cubiertos por instrumentación.
- [x] Screenshots y fixtures temporales eliminados; no se commitearon artefactos visuales.
- [ ] Instalación/reinstalación física limpia con prompts reales y credenciales.
- [ ] FCM real con dos usuarios en foreground, background y app terminada.
- [ ] Recorrido manual físico completo con TalkBack y font scaling.

La validación física con credenciales y notificaciones reales continúa pendiente; el PR permanece draft.

## 🧾 Commits incluidos

- `b1eba` `fix(permissions): request notifications during onboarding`
- `5593e` `refactor(navigation): replace drawer with account sheet`
- `d7d4a` `feat(ui): add zibe motion and interaction feedback`
- `87153` `feat(discover): migrate people discovery to compose`
- `12e08` `fix(navigation): use destination ids for root navigation`
- `4093a` `test(ui): cover permissions discover and navigation`
- `d0508` `fix(ui): address final review findings`
- `79fdd` `test(permissions): cover launcher sequencing and restoration`
- `154c6` `fix(settings): use current Hilt Compose API`
- `bb1d1` `test(discover): wait for committed search input`
- `62c11` `docs(architecture): document ui navigation and motion`
- `c70d1` `docs(pr): describe ui modernization`
- `70934` `fix(ui): align account avatar and interaction feedback`
- `e7c8b` `refactor(ui): share profile status tags`
- `a92e5` `feat(discover): restore zibe person card design in compose`
- `9dc31` `fix(discover): restore toolbar search filters and scroll to top`
- `9a100` `fix(discover): expose active filter tint state`
- `c79cc` `test(ui): cover discover visual corrections`
- `7524f` `fix(discover): restore legacy card typography`
- `d9e06` `docs(pr): document discover visual corrections`
- `ca95b` `test(discover): await terminal first contact state`
- `9cb45` `docs(pr): record discover ci follow-up`
- `deb98` `fix(ui): simplify discover filters and bottom navigation`
- `03dd5` `refactor(dm): share entry gate across discover and profile`
- `52c8b` `fix(chat): integrate toolbar with gradient background`
- `c7f77` `test(ui): cover dm entry and chrome consistency`
- `28ffd` `docs(pr): record cross-screen consistency corrections`
- `test(profile): await metadata before harness completion` (commit que contiene el follow-up de CI).

## ⚠️ Riesgos y pendientes

- La instrumentación y la revisión visual en emulador no reemplazan prompts OEM, credenciales reales, TalkBack físico ni FCM con dos cuentas.
- Permanecen warnings baseline de APIs deprecadas y auditoría npm fuera del alcance; no se agregaron suppressions ni se modificó el toolchain.
- El PR permanece draft hasta completar la validación física/externa pendiente.

## 🚫 Fuera de alcance

- Backend de Salas, Functions, Rules, FCM y contrato de escritura/receipts DM.
- Permisos, onboarding, Settings de notificaciones, dependencias, toolchain y SDK levels.
- Merge, deploy Firebase, release o tag.

## 🚫 Sin deploy

No se desplegó Firebase ni ningún servicio remoto. No se mergeó, publicó release ni creó tag.
