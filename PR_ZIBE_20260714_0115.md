# ✨ feat(ui): modernizar navegación, Descubrir y permisos

**PR:** #52  
**Base:** `main` @ `73bbfb36ddf5531603bd3be178d0d87ad7ecf298`  
**Rama:** `feature/ui-motion-discover-permissions`  
**Estado:** abierto, mergeable y en draft  
**Último commit funcional:** `cc326285092d14e014e7866d5a0b3743a758fc8b`

## Resumen ejecutivo

Este PR completa una primera vertical de modernización de ZIBE sin modificar los contratos de Firebase ni el flujo DM consolidado en el PR #51.

El resultado final:

- corrige la solicitud de notificaciones en instalaciones nuevas y usuarios existentes;
- elimina el drawer y mueve Cuenta al avatar de la toolbar;
- mantiene `Chats` como inicio y deja la navegación raíz en `Descubrir`–`Chats`–`Salas`–`Favoritos`;
- migra Descubrir a Compose conservando el lenguaje visual original de PERSONAS;
- unifica el aviso previo al primer DM desde Descubrir y Perfil;
- integra la toolbar de ChatActivity con el gradiente de ZIBE;
- mantiene feedback Material estándar, sin motion ni háptica parcial.

## Cambios principales

### Permisos y onboarding

- Solicitud secuencial: ubicación primero y notificaciones después en Android 13+.
- Ubicación continúa siendo obligatoria; notificaciones son opcionales y no bloquean el acceso.
- Usuarios existentes con ubicación concedida y notificaciones nunca solicitadas reciben el paso específico correspondiente.
- Una negativa previa no dispara solicitudes automáticas en cada inicio.
- Ajustes ofrece recuperación explícita y refresca el estado real al regresar.
- Se eliminó el requester singleton que podía retener un launcher asociado a una Activity destruida.
- No se modificaron FCM, Functions, Rules ni payloads.

### Navegación y Cuenta

- Drawer y hamburger eliminados completamente.
- Avatar propio en toolbar con borde blanco de `1.dp` y acceso al sheet de Cuenta.
- Cuenta conserva Editar perfil, Ajustes y Cerrar sesión.
- Destinos raíz: `Descubrir`, `Chats`, `Salas`, `Favoritos`.
- `Chats` sigue siendo el start destination.
- Bottom navigation icon-only; títulos, content descriptions, selección y badges siguen disponibles para accesibilidad y estado.

### Descubrir

- Pantalla Compose dentro de `UsersFragment`; no se restauró RecyclerView ni el adapter legacy.
- Cards glass `#1AFFFFFF`, radio `20.dp`, avatar circular de `70.dp`, borde blanco y status dot online/offline.
- Nombre y edad con Poppins `18sp`; descripción Poppins `13sp`, una línea y ellipsis.
- Edad en formato `%d años` y fallback `Perfil eliminado`.
- `UserStatusTag` compartido con Perfil para distancia, favorito, bloqueos, silenciado y coincidencia de sala.
- Eliminada la estrella editable de Descubrir; Favoritos continúa administrándose desde Perfil.
- Búsqueda y filtro viven en la toolbar; no existe refresh redundante en toolbar.
- Online y edad se configuran en un único bottom sheet.
- Botón `Volver arriba` restaurado con `animateScrollToItem(0)`.
- Se eliminó por completo la sección `Para vos` y su espacio.
- Pull-to-refresh, retry, estados loading/error/empty y primer contacto permanecen operativos.

### Estado vacío y filtros

- El mensaje `No se encontraron personas con estos criterios` usa padding horizontal mediante el token `screen_padding` y alineación centrada.
- `Limpiar filtros` utiliza el botón primario violeta/accent, no una variante transparente.
- El rango de edad usa color claro explícito.
- `Cancelar` y `Aplicar filtros` quedan fijos y visibles al abrir; sólo el contenido central scrollea.
- El sheet respeta navigation bar, IME, altura compacta y font scaling.

### Entrada a DM desde Descubrir y Perfil

- `ResolveDmEntryUseCase` centraliza la decisión:
  - conversación existente → abre directamente;
  - conversación nueva → requiere confirmación;
  - error o resultado inválido → snackbar y cero navegación.
- `FirstContactSheet` es un único componente compartido por Descubrir y Perfil.
- No se crea chat, mensaje ni escritura previa a la confirmación.
- `ProfileViewModel` evita doble toque y conserva el UID explícito.
- El pager cancela consultas de páginas inactivas y evita resultados cruzados.
- `NODE_GROUP_DM` y accesos desde conversaciones persistidas permanecen sin cambios.

### ChatActivity

- `ChatTopBar` y status bar transparentes para dejar visible el gradiente.
- Modo normal y selección comparten el mismo chrome transparente.
- Se retiró `navigationBarsPadding()` de la barra superior.
- Nombre, estado, back, avatar, perfil, overflow y selección permanecen operativos.
- `en línea`, `últ. vez…` y estados equivalentes usan blanco para asegurar contraste.

### Política de interacción

- Se retiraron scale, sinking, bounce y háptica personalizada agregados de forma parcial.
- La app usa ripple y feedback Material estándar.
- `ZibeMotion.kt` fue eliminado al quedar sin consumidores.

## Arquitectura y contratos preservados

- UI → ViewModel/use case → repository; sin Firebase desde Activities, Fragments o Composables.
- `ResolveDmEntryUseCase` vive en `domain` y reutiliza el repositorio existente.
- Se preservan Hilt, `ZibeResult`, StateFlow y eventos one-shot adoptados por el repositorio.
- Sin cambios en paths RTDB, Functions, Rules, payloads FCM ni backend.
- Se preservan:
  - `MSG_DELIVERED = 1`;
  - `MSG_RECEIVED = 2`;
  - `MSG_SEEN = 3`;
  - monotonicidad;
  - fan-out atómico;
  - server timestamps;
  - unread y resúmenes DM;
  - receipt Android real;
  - seen del thread activo;
  - checks de chat y chatlist;
  - soft-delete, tombstones y compatibilidad legacy documentada.

## Archivos principales

- `app/src/main/java/com/zibete/proyecto1/ui/users/`
- `app/src/main/java/com/zibete/proyecto1/ui/main/`
- `app/src/main/java/com/zibete/proyecto1/ui/profile/`
- `app/src/main/java/com/zibete/proyecto1/ui/chat/`
- `app/src/main/java/com/zibete/proyecto1/ui/components/UserStatusTag.kt`
- `app/src/main/java/com/zibete/proyecto1/ui/components/FirstContactSheet.kt`
- `domain/src/main/java/com/zibete/proyecto1/domain/chat/ResolveDmEntryUseCase.kt`
- `app/src/main/res/menu/menu_main.xml`
- `app/src/main/res/layout/content_main.xml`
- `docs/ARCHITECTURE.md`

Código eliminado por quedar obsoleto:

- drawer y recursos exclusivos;
- RecyclerView/adapters/layouts legacy de Personas;
- acción de favorito desde Descubrir;
- consumidores de motion/háptica parcial;
- `core/designsystem/.../motion/ZibeMotion.kt`.

## Tests y validación

### Resultado local completo

- Arquitectura: OK.
- Functions: compilación Python OK; `6/6` tests.
- Firebase Rules: `49/49`.
- JVM: `115/115`, sin skips.
- Instrumentation: `60/60` en Pixel_6a API 37, sin skips.
- AndroidTest lint model y grafo Hilt: OK.
- Lint: 0 errores; warnings históricos fuera del alcance.
- Debug APK: OK.
- Release Kotlin: OK.
- APK compatible con páginas de 16 KB: verificado.
- `git diff --check`: OK.

Comandos principales ejecutados:

```bash
python scripts/check_architecture.py
python -m py_compile functions/main.py
python -m unittest discover functions/tests
npm ci
npm run test:rules
gradlew.bat testDebugUnitTest --no-daemon
gradlew.bat :app:generateDebugAndroidTestLintModel :app:compileDebugAndroidTestKotlin --no-daemon
gradlew.bat lintDebug --rerun-tasks --no-daemon
gradlew.bat :app:assembleDebug --no-daemon
gradlew.bat :app:compileReleaseKotlin --no-daemon
gradlew.bat :app:connectedDebugAndroidTest --no-daemon
zipalign -c -P 16 -v 4 app/build/outputs/apk/debug/app-debug.apk
git diff --check
```

### Cobertura relevante

- Permisos: secuencia, grant/deny, usuario existente, restauración y ausencia de requests duplicados.
- Descubrir: cards, presencia, tags, búsqueda, filtros, estados, scroll-to-top y primer contacto.
- Navegación: Chats inicial, icon-only, badges, Cuenta, intents DM y back stack.
- Filtros: footer fijo, contenido scrollable, contraste y acciones.
- Perfil: gate DM, doble toque, error, pager y UID correcto.
- Chat: toolbar/status bar transparentes, selección, overflow, back y acceso al perfil.

## Auditoría de consistencia

El auditor inicial detectó:

- bypass del aviso de primer contacto desde Perfil;
- posible resultado cruzado entre páginas del pager;
- footer del filtro fuera de pantalla;
- contraste insuficiente en filtros y ChatActivity.

Todos los hallazgos bloqueantes e importantes fueron corregidos. La auditoría final cerró con **0 bloqueantes y 0 importantes**.

## Validación visual y manual

Completado:

- comparación con las capturas de PERSONAS y DESCUBRIR;
- cards glass, tipografía, avatar, presencia, tags y estado vacío;
- toolbar de MainActivity y navegación icon-only;
- filter sheet sin clipping y con acciones visibles;
- aviso compartido desde Descubrir y Perfil;
- ChatTopBar transparente y estado en blanco;
- screenshots y fixtures temporales eliminados antes de commitear.

Pendiente externo:

- instalación/reinstalación física con prompts OEM y credenciales reales;
- recorrido físico completo con TalkBack y font scaling;
- FCM real con dos cuentas en foreground, background y aplicación terminada.

Por estas validaciones externas pendientes, el PR continúa en draft.

## CI remoto

- Último commit funcional validado: `cc326285092d14e014e7866d5a0b3743a758fc8b`.
- Android CI run `29621670438`: `build-test-lint` finalizó correctamente.
- Architecture, Functions, Rules, JVM, androidTest model, Hilt graph, lint, assemble, 16 KB alignment y release compile: PASS.

## Commits incluidos

<details>
<summary>Ver historial completo</summary>

- `b1eba` `fix(permissions): request notifications during onboarding`
- `5593e` `refactor(navigation): replace drawer with account sheet`
- `d7d4a` `feat(ui): add zibe motion and interaction feedback`
- `87153` `feat(discover): migrate people discovery to compose`
- `12e08` `fix(navigation): use destination ids for root navigation`
- `4093a` `test(ui): cover permissions discover and navigation`
- `d0508` `fix(ui): address final review findings`
- `79fdd` `test(permissions): cover launcher sequencing and restoration`
- `154c6` `fix(settings): use current Hilt Compose API`
- `bb1d1` `test(discover): wait for committed search input`
- `62c11` `docs(architecture): document ui navigation and motion`
- `c70d1` `docs(pr): describe ui modernization`
- `70934` `fix(ui): align account avatar and interaction feedback`
- `e7c8b` `refactor(ui): share profile status tags`
- `a92e5` `feat(discover): restore zibe person card design in compose`
- `9dc31` `fix(discover): restore toolbar search filters and scroll to top`
- `9a100` `fix(discover): expose active filter tint state`
- `c79cc` `test(ui): cover discover visual corrections`
- `7524f` `fix(discover): restore legacy card typography`
- `d9e06` `docs(pr): document discover visual corrections`
- `ca95b` `test(discover): await terminal first contact state`
- `9cb45` `docs(pr): record discover ci follow-up`
- `deb98` `fix(ui): simplify discover filters and bottom navigation`
- `03dd5` `refactor(dm): share entry gate across discover and profile`
- `52c8b` `fix(chat): integrate toolbar with gradient background`
- `c7f77` `test(ui): cover dm entry and chrome consistency`
- `28ffd` `docs(pr): record cross-screen consistency corrections`
- `77b84` `test(profile): await metadata before harness completion`
- `cc326` `fix(ui): improve empty state and chat status contrast`
- `853fcd9` `docs(pr): finalize ui modernization summary`

</details>

## Riesgos y fuera de alcance

Riesgo residual:

- validaciones físicas y externas pendientes;
- warnings baseline de APIs deprecadas y auditoría npm no introducidos por este PR.

Fuera de alcance:

- backend público de Salas;
- reacciones;
- cambios de Functions, Rules, FCM o contratos RTDB;
- Navigation 3;
- actualización de dependencias, toolchain o SDK levels;
- merge, deploy, release o tag.

## Estado final

- PR #52 abierto, mergeable y draft.
- Rama sincronizada con remoto.
- Sin revert, reset, rebase, squash, amend ni force-push.
- Sin merge.
- Sin deploy de Firebase.
- Sin release ni tag.
