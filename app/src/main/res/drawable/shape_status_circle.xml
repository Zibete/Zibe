<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/status_offline" />
    <stroke
        android:width="@dimen/status_indicator_stroke"
        android:color="@color/status_stroke" />
</shape>
