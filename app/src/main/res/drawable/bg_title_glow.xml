<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Capa de resplandor -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="30dp"/>
            <gradient
                android:type="radial"
                android:centerX="0.5"
                android:centerY="0.5"
                android:gradientRadius="70dp"
                android:centerColor="#66FFFFFF"
                android:endColor="#00FFFFFF"/>
        </shape>
    </item>

    <!-- Tu botón/título original (ligeramente inset para que se vea el glow) -->
    <item
        android:top="2dp" android:bottom="2dp"
        android:left="2dp" android:right="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="30dp" />
            <stroke android:width="3dp" android:color="@color/blanco"/>
            <solid android:color="@color/zibe_text_light"/>
        </shape>
    </item>

</layer-list>
