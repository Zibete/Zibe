# Entrega Salas — avance y punto de reanudación

Actualizado: 2026-09-05 (Argentina). Estado: etapa 1 validada; etapa 2 en curso; DoD pendiente.

- Checkout confirmado: `C:\Users\pc\Documents\ZIBE`, remoto `https://github.com/Zibete/Zibe.git`, rama `feature/rooms-end-to-end`, HEAD inicial `ef370a2add968b0d3bf393723917a29eead1e06a`. Índice y árbol inicialmente limpios. Sin cambios previos que preservar.
- Restricciones: trabajo local; sin push, PR remoto, deploy, migración ni acceso a producción. Datos y cuentas de prueba ficticios, proyecto obligatorio `demo-zibe-rooms`.
- Entorno: Windows nativo/PowerShell 7.6.5; JDK 17.0.16.8 explícito (PATH tenía Java 11), Gradle del repo, SDK `%LOCALAPPDATA%\Android\Sdk`. WHPX disponible. AVD existente Pixel_6a/API 37.0 x86_64/16 KB, serial `emulator-5556`; boot completo comprobado. No se borró/resetearon dispositivos; arranque sin guardar snapshot.
- Línea base: arquitectura OK; Functions 19/19; Rules 76/76. `:app:assembleDebug :app:compileDebugAndroidTestKotlin testDebugUnitTest --no-daemon` OK; tareas JVM estaban UP-TO-DATE, no cuentan como ejecución nueva. Evidencia local no versionada: `build/rooms-evidence/baseline/`.
- Causas confirmadas: sesión única elimina membresía al cambiar sala; aliases, participantes y mensajes exponen UID; privados legacy dependen del par UID y no de sala/identidad. Configuración emuladores solo RTDB; tests UI existentes usan fakes. No se inspeccionó producción; PERMISSION_DENIED remoto es antecedente.
- Decisión de contrato: nueva experiencia sobre esquema aislado RoomsV2, identidad contextual opaca y mapa privado server-owned; conservar Groups/group_dm y DM normal. Membresía persistente separada de navegación/presencia. Contrato detallado e implementación aún pendientes.
- Decisión de entorno: buildType local con applicationId distinto, bootstrap demo antes del primer SDK/DI, Auth/RTDB/Storage/Functions locales obligatorios. FCM/Installations reales quedan fuera del runtime local. Instrumentation local separado de módulos Hilt fake.
- División: auditorías Android/backend/pruebas completadas. Los agentes dejaron cambios parciales de etapa 1 y luego fallaron por límite de uso; el responsable principal integró y continúa. No hay agentes editando ni otros Gradle corriendo.
- Secuencia de commits prevista: (1) entorno reproducible y prueba integrada mínima; (2) contrato/seguridad backend; (3) corte vertical UI/repositorio y smoke; (4) multisala/identidades/privados/lectura; (5) multimedia/moderación/notificaciones; (6) regresión y documentación final. Cada commit condicionado a validación de su alcance.
- Etapa 1 actual: variante `local`/`.local`, bootstrap previo a Hilt, UI sin proveedores externos locales, repositorio de sesión real con instalación local, scripts `rooms-local.ps1`/`rooms-fixtures.py`, Functions Python313/SDK0.6.0/Admin7.4.0 y CLI15.5.1 existentes. Java25 solo Firebase, Java17 Gradle. Backend actual registrado en `build/rooms-evidence/local/processes.json`; AVD inicial sigue vivo y es ajeno al registro del script (no detenerlo globalmente).
- Namespace obligatorio comprobado: `demo-zibe-rooms-default-rtdb`. CLI cargaba Rules aquí mientras Functions descubrimiento usaba otro default; se fija `DATABASE_URL` para el proceso CLI y se compara la Rules cargada con el archivo del checkout antes de probar. Android FirebaseOptions usa URL HTTP local explícita con `ns`, evitando que llamadas repetidas a `getInstance()` vuelvan al host original después de `useEmulator`.
- Primera integración aprobada: 2/2 pruebas instrumentadas reales en `emulator-5556` (Auth ficticia, repositorio de sesión, allow/deny RTDB, Storage, Functions; login UI → Main → Salas). Informe `app/build/outputs/androidTest-results/connected/local/`; log `build/rooms-evidence/local/bootstrap-tests.log`. Captura inspeccionada visualmente: `build/rooms-evidence/local/screenshots/local-login-rooms.png`, estado vacío legacy; no representa UI final.
- Causa adicional confirmada por UI: `clearSession` borraba el padre no autorizado por Rules. Ahora borra sus dos campos con update atómico; prueba allow/deny de regresión. Intentos fallidos de bootstrap preservados en evidencia, incluyendo namespace abierto incorrecto detectado por deny; todos corregidos antes del pase.
- Validación etapa 1: Functions 33/33 (`build/rooms-evidence/local/functions.xml`), Rules 77/77 (`rules.xml`), JVM app 121/domain 33/core-common 6, sin fallos/skips; lintLocal y compileReleaseKotlin OK (`stage1-validation.log`). Arquitectura y whitespace OK. Compilación productiva sin ejecución ni acceso remoto. Pendiente chequeo 16 KB y matriz integrada final.
- Próximo paso: implementar contrato RoomsV2, permisos y concurrencia con SDK autenticado; luego corte vertical UI/repositorio y smoke completo. Comando actual instrumentado: `pwsh -NoProfile -File scripts/rooms-local.ps1 -Action Test -Serial emulator-5556`. El smoke de Salas se agregará con el corte vertical, no está implementado aún.
- Pendiente externo explícito de la entrega: FCM real vía Google en Firebase de pruebas, background/proceso muerto/retirada de recientes/force-stop diferenciados; requiere autorización futura y no se sustituye por Emulator Suite.
