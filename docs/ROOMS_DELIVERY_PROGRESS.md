# Entrega Salas — avance y punto de reanudación

Actualizado: 2026-09-05/06 (Argentina). Estado: etapa 1 validada; etapa 2 backend/contratos parcialmente implementada; DoD pendiente.

- Checkout confirmado: `C:\Users\pc\Documents\ZIBE`, remoto `https://github.com/Zibete/Zibe.git`, rama `feature/rooms-end-to-end`, HEAD base de esta continuación `fb962d3371a36757d3d2672e3c9ff60a51f29072` (`feat(rooms): add local Firebase test infrastructure and rooms v2 groundwork`). El pedido actual autoriza commit/push sobre esta misma rama; no autoriza deploy ni cambios en producción.
- Entorno base ya validado por la etapa 1: Windows/PowerShell, JDK 17 Gradle, Firebase local, AVD Pixel_6a serial `emulator-5556`. Evidencia local previa: 2/2 instrumentation reales; Functions 33/33; Rules 77/77; JVM app 121/domain 33/core-common 6; lintLocal y compileReleaseKotlin OK. Es evidencia del checkpoint anterior, no una reejecución de los cambios V2 actuales.
- Causas confirmadas: sesión única legacy obliga a abandonar al cambiar sala; aliases/participantes/mensajes legacy exponen UID; privados legacy dependen del par UID y no de sala/identidad. `GroupsViewModel` y `RoomUseCases` siguen imponiendo `SwitchRequired`, por lo que todavía no se corta la UI al nuevo backend.
- Decisión vigente: nueva experiencia sobre esquema aislado `RoomsV2`, identidad contextual opaca y mapeo privado server-owned; conservar `Groups`, `group_dm` y DM normal hasta completar el corte vertical. Membresía persistente queda separada de navegación y presencia.
- RoomsV2 implementado en esta etapa: contratos de dominio, validación compartida de nombre/alias/mensaje/clientMessageId, repositorio Firebase paralelo, binding Hilt y callables autenticadas de crear/ingresar/salir/chat texto. Se sumaron cierre de sala, transferencia de owner a identidad real activa, designación/revocación de moderador y expulsión/ban con jerarquía backend.
- Esquema V2: `publicRooms`, `publicMembers`, `publicMessages`, `membershipIndexByUser` y rama `private` server-only con `userIdentity`, `identityOwners`, `aliasClaims`, `messageClaims` y `bans`. Los árboles públicos usan IDs opacos; el UID no forma parte de la identidad anónima pública.
- Concurrencia: alias anónimo usa claim transaccional; mismo uid+sala+alias recupera `identityId`, otra cuenta activa obtiene `ALREADY_EXISTS`; alias liberado puede ser reclamado sin heredar la identidad ajena. `clientMessageId` usa claim transaccional e idempotente para reutilizar `messageId` en reintentos.
- Escrituras V2: el cliente no escribe directamente. Functions realiza validación/autorización y fan-out RTDB. `memberCount` es contador derivado actualizado con transacción separada y requiere reconciliación antes del DoD; no es boundary de autorización.
- Compatibilidad Functions: el `main.py` previo se conserva byte por byte como `legacy_main.py`; el nuevo entrypoint vuelve a exponer las Functions legacy en el módulo `main` y agrega las callables V2. Esto conserva paths y permite que los tests legacy sigan parcheando helpers del módulo sin reescribir DM/notificaciones.
- Rules V2: ruleset dedicado `database.roomsv2.rules.json`, deny-by-default, directorio solo autenticado, membresías solo del propio uid, contenido solo para miembros activos, `private` inaccesible y todos los writes de cliente denegados. Se prueba de forma aislada con `firebase.roomsv2.local.json`/puerto 9001 para no modificar aún `database.rules.json` productivo.
- Validación ejecutada en esta continuación: 8/8 tests Python puros de validación/claims/jerarquía; `py_compile` de `rooms_v2.py`, `rooms_v2_moderation.py`, `rooms_v2_core.py` y entrypoint; compilación Kotlin aislada de contratos/validación/use cases V2; sintaxis JS de la suite Rules V2 y JSON del ruleset. No se pudo ejecutar Firebase Emulator/Gradle/AVD desde este runtime remoto, por lo que build Android integrado, Functions SDK real, Rules Emulator y smoke V2 quedan pendientes de reejecución en el equipo.
- No se corta todavía `GroupsFragment`/`GroupsViewModel` ni `GroupHost` a V2: hacerlo antes de integrar Rules V2 al backend local completo dejaría la UI apuntando a un backend parcial. El próximo corte debe integrar el fragmento de Rules al ruleset local completo, ejecutar legacy+V2, y recién después migrar listado/crear/ingresar/chat texto a `RoomsV2Repository`.
- Pendientes mayores después del corte texto: UI de roles/transferencia/cierre/moderación, reportes y evidencia, privados contextuales V2, lectura/unread, multimedia/Storage, presencia, notificaciones V2 y FCM real autorizado. FCM real sigue expresamente fuera de la validación local.
